package tokens;

public enum TokenType {
    L_PAREN,
    SEMICOLON,
    R_PAREN,
    EQUALS,
    LET,
    STR_LITERAL,
    IDENTIFIER,
    SUM,
    MINUS,
    MULTIPLY,
    NUM_LITERAL,
    DIVIDE,
    PRINT,
    UNKNOWN,
    NEW_LINE,
    SPACE,
    COLON,
}
